\input{style/preamble-general.tex}
\input{style/preamble-font.tex}
\input{style/preamble-layout.tex}

\usepackage[a-2b]{pdfx}
\usepackage[bottom]{footmisc}

%\flushbottom

%\fnbelowfloat

\renewcommand{\floatpagefraction}{.55}

\setlength{\marginparwidth}{0pt}
\setlength{\marginparsep}{0pt}

\DeclareCaptionStyle{widefigure}{font=normalsize, margin=0cm}
\DeclareCaptionStyle{widetable}{font=normalsize, margin=0cm}

\setlength{\tabcolsep}{4pt}

\makeatletter
% move marginnotes to footnote area
\renewcommand\marginnote[1]{
    \begingroup
    \renewcommand\thefootnote{}\footnote{#1}
    \addtocounter{footnote}{-1}
    \endgroup
    \@afterindentfalse\@afterheading
}
\makeatother

\renewenvironment{marginfigure}{
    \begin{figure}[tbp]
}{
    \end{figure}
}
